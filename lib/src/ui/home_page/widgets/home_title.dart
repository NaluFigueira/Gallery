import 'package:flutter/material.dart';

class HomeTitle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("Welcome back, Ana",
        style: TextStyle(
          fontSize: 30,
          color: Colors.black45,
        ));
  }
}
